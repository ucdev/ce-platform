require(["vendor/jquery/jquery", "vendor/jquery/jquery.alpha", "vendor.jquery.jquery.beta"], function($) {
    //the jquery.alpha.js and jquery.beta.js plugins have been loaded.
    $(function() {
        $('body').alpha().beta();
    });
});
