#! ce._core.pagers.<%= loc.nameInPluralUppercase =%> extends Backbone.Collection */
ce.module "_core.pagers", (self, ce, Backbone, Marionette, $, _, models) ->
	self.<%= loc.nameInPluralUppercase =%> = Backbone.Paginator.clientPager.extend
		initialize: ->
		paginator_core:
			type: 'post'
			dataType: 'json'
			url:'/<%= loc.nameInPluralLowercase =%>/'
		paginator_ui:
			firstPage: 1
			currentPage: 1
			perPage: 15
		parse: (response) ->
			@totalPages = Math.ceil(response.length / @perPage)
			return response
		model: models.<%= loc.nameInSingularUppercase =%>
,ce._core.models