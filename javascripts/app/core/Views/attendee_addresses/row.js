#! ce._core.views.attendee_addresses.RowView extends Backbone.Marionette.ItemView 
ce.module "_core.views.attendee_addresses", (self, ce, Backbone, Marionette, $, _) ->
  self.RowView = Backbone.Marionette.ItemView.extend(
    tagName: "tr"
    template: "attendee_addresses/row"
    events:
      "click .destroy": "destroy"
      "click .edit": "edit"

    
    # delete: activity
    destroy: ->

    
    # edit: activity
    edit: ->
  )

