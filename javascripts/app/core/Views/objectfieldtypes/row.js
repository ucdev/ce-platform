#! ce._core.views.objectfieldtypes.RowView extends Backbone.Marionette.ItemView 
ce.module "_core.views.objectfieldtypes", (self, ce, Backbone, Marionette, $, _) ->
  self.RowView = Backbone.Marionette.ItemView.extend(
    tagName: "tr"
    template: "objectfieldtypes/row"
    events:
      "click .destroy": "destroy"
      "click .edit": "edit"

    
    # delete: activity
    destroy: ->

    
    # edit: activity
    edit: ->
  )

