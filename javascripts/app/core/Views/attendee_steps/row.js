#! ce._core.views.attendee_steps.RowView extends Backbone.Marionette.ItemView 
ce.module "_core.views.attendee_steps", (self, ce, Backbone, Marionette, $, _) ->
  self.RowView = Backbone.Marionette.ItemView.extend(
    tagName: "tr"
    template: "attendee_steps/row"
    events:
      "click .destroy": "destroy"
      "click .edit": "edit"

    
    # delete: activity
    destroy: ->

    
    # edit: activity
    edit: ->
  )

