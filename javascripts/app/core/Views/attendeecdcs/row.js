#! ce._core.views.attendeecdcs.RowView extends Backbone.Marionette.ItemView 
ce.module "_core.views.attendeecdcs", (self, ce, Backbone, Marionette, $, _) ->
  self.RowView = Backbone.Marionette.ItemView.extend(
    tagName: "tr"
    template: "attendeecdcs/row"
    events:
      "click .destroy": "destroy"
      "click .edit": "edit"

    
    # delete: activity
    destroy: ->

    
    # edit: activity
    edit: ->
  )

