define("ce", ["jquery", "jqueryui", "underscore", "backbone"], function(require, module, exports) {
  var app;
  app = app || {};
  console.log("bootloader");
  console.dir(_);
  return exports.ce = app;
});