/*! ce.Views.<%= loc.nameInPluralUppercase =%>.IndexView extends Backbone.Marionette.CompositeView */
ce.pkg("Views.<%= loc.nameInPluralUppercase =%>",function(self,ce,Backbone,Marionette,$,_) {
	self.IndexView = Backbone.Marionette.CompositeView.extend({
		tagName: 'table',
		itemView: self.RowView,
		template: '<%= loc.nameInPluralLowercase =%>/index',
		
		events: {
			'click .new': 'newRecord'
		},
		
		appendHtml: function(collectionView, itemView) {
			collectionView.$('.<%= loc.nameInPluralLowercase =%>').append(itemView.el);
		},
		
		newRecord:function() {
				
		}
	});
});