/*! app/models/<%= loc.nameInSingularLowercase =%> 
* 	@requires: app
* 	@extends: app.Model
* 	@exports: app.models.<%= loc.nameInSingularUppercase =%>
*/
define("app/models/<%= loc.nameInSingularLowercase =%>",["require","app"],function(require,app) {
	var <%= loc.nameInSingularUppercase =%>Model = app.Model.extend({
		url: function() {
			base = "/api/<%= loc.nameInPluralLowercase =%>/"
            
            if(this.isNew()) {
                return base
            } else {
                return base + this.id
            }
		}
	});

	module.setExports(<%= loc.nameInSingularUppercase =%>Model)
});