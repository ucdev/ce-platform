/*! app/models/<%= loc.nameInSingularLowercase =%> 
* 	@requires: app/model
* 	@extends: app.Model
* 	@exports: app.models.<%= loc.nameInSingularUppercase =%>
*/
define("app/models/<%= loc.nameInSingularLowercase =%>",["require"],function(require) {
	var AppModel = require("app/model");
	var <%= loc.nameInSingularUppercase =%>Model = AppModel.extend({
		url: function() {
			base = "/api/<%= loc.nameInPluralLowercase =%>/"
            
            if(this.isNew()) {
                return base
            } else {
                return base + this.id
            }
		}
	});

	module.setExports(<%= loc.nameInSingularUppercase =%>Model)
});