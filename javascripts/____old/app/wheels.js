// Generated by CoffeeScript 1.3.3

ce.module("wheels", function(self, ce, Backbone, Marionette, $, _) {
  return self.go = function(params) {
    var defaults, settings;
    defaults = {
      pjax: true,
      container: ".contentArea"
    };
    settings = $.extend({}, defaults, params);
    if (ce.isBlank(settings.pjax)) {
      throw "argument 'pjax' should be set to a boolean or excluded completely";
    }
    if (settings.pjax && ce.isBlank(settings.container)) {
      throw "argument 'container' is required if 'pjax' = true";
    }
    if (settings.pjax === true) {
      $.pjax({
        'url': settings.url,
        'container': settings.container
      });
      return false;
    } else {
      window.location(settings.url);
    }
  };
});
