// Generated by CoffeeScript 1.3.3

ce.module("_core.pagers", function(self, ce, Backbone, Marionette, $, _, models) {
  self.clientActivity_cdc = Backbone.Paginator.clientPager.extend({
    initialize: function() {},
    paginator_core: {
      type: 'post',
      dataType: 'json',
      url: '/activity_cdc/loadData'
    },
    paginator_ui: {
      firstPage: 1,
      currentPage: 1,
      perPage: 15
    },
    parse: function(response) {
      this.totalPages = Math.ceil(response.length / this.perPage);
      return response;
    },
    model: models.Activity_cdc
  });
  return self.requestActivity_cdc = Backbone.Paginator.requestPager.extend({
    initialize: function() {},
    paginator_core: {
      type: 'post',
      dataType: 'json',
      url: '/activity_cdc/loadData'
    },
    paginator_ui: {
      firstPage: 1,
      currentPage: 1,
      perPage: 15
    },
    parse: function(response) {
      this.totalPages = Math.ceil(response.length / this.perPage);
      return response;
    },
    model: models.Activity_cdc
  });
}, ce._core.models);
